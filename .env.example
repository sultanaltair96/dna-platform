# Environment variables for dna-platform

# Project settings
ENV=dev
BASE_PATH=/src/data/dev

# Storage configuration
STORAGE_BACKEND=local

# ADLS configuration (uncomment and fill if using ADLS)
# ADLS_ACCOUNT_NAME=your_storage_account
# ADLS_CONTAINER=your_container
# ADLS_BASE_PATH=polster/data
# ADLS_ACCOUNT_KEY=your_account_key

# Dual-write configuration (when using ADLS)
# When ADLS is configured, full data is written to ADLS and a sample is written locally
# DUAL_WRITE_SAMPLE_SIZE=50        # Number of rows to keep locally (default: 50)
# DISABLE_LOCAL_SAMPLE=false       # Set to 'true' to skip local samples entirely
# ADLS_FALLBACK_TO_LOCAL=false     # Set to 'true' to write full data locally if ADLS fails

# Dagster configuration
DAGSTER_HOME=.dagster